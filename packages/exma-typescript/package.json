{
  "name": "exma-typescript",
  "version": "0.0.7",
  "scripts": {
    "build": "npm run build:tsc && npm run build:files",
    "build:tsc": "tsc",
    "build:files": "cp ./README.md ./dist/ && cp ./package.json ./dist/ && cp ./LICENSE ./dist/"
  },
  "dependencies": {
    "exma": "0.0.7",
    "ts-morph": "18.0.0"
  },
  "devDependencies": {
    "@types/node": "20.2.5",
    "ts-node": "10.9.1",
    "typescript": "5.0.4"
  }
}
